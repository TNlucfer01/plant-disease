# ─────────────────────────────────────────────────────────────────────────────
# Plant Disease Detection – Python dependencies
# RTX 5050 (Blackwell sm_120) REQUIRES PyTorch ≥ 2.7 + CUDA 12.8
#
# Install with:
#   pip install -r requirements.txt --index-url https://download.pytorch.org/whl/cu128
#
# Or install PyTorch separately first (recommended):
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu128
#   pip install -r requirements.txt
# ─────────────────────────────────────────────────────────────────────────────

# Core – CUDA build required for RTX 5050 (Blackwell)
torch>=2.7.0
torchvision>=0.22.0
torchaudio>=2.7.0
Pillow>=10.0.0

# ONNX export & inference
onnx>=1.14.0
onnxruntime-gpu>=1.20.0   # GPU-accelerated inference
onnxscript>=0.1.0

# HTTP / LLM integration
requests>=2.31.0

# UI
streamlit>=1.28.0

# Data processing
numpy>=1.24.0

# Training utilities
tqdm>=4.66.0
matplotlib>=3.7.0
scikit-learn>=1.3.0
seaborn>=0.13.2

# Production and API deployment
gunicorn>=21.2.0
fastapi>=0.104.0
uvicorn>=0.24.0
python-multipart>=0.0.9
